version: "3.8"
services:
  api:
    build:
      context: .
      dockerfile: ./Dockerfile
    restart: always
    ports:
      - 5000:5000
    networks:
      - pedtet
    environment:
      PORT: 5000
      APP_ENV: devlocal
      BUILD_VERSION: local-build

networks:
  pedtet: {}
